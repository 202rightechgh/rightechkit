# Docs

RightTeKit keeps docs intentionally small to start with.

- top-level usage lives in the project `README.md`
- scaffold behavior is tested in `tests/`
- command behavior is implemented under `righttekit/cli/`

As the toolkit grows, this directory can expand into command guides and module reference docs.

