# {{ project_name }}

Generated with RightTeKit.

Service slug: `{{ project_slug }}`

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e '.[dev]'
right launch --reload
```
